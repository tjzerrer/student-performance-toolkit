---
import BaseLayout from "../layouts/BaseLayout.astro";

const siteUrl = "https://student-performance-toolkit-6hgn.vercel.app";
const pageUrl = `${siteUrl}/weighted-grade-calculator`;

const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  mainEntity: [
    {
      "@type": "Question",
      name: "How do I calculate a weighted grade?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Multiply each category grade by its weight, add those weighted values together, and divide by the total weight. This calculator does that automatically."
      }
    },
    {
      "@type": "Question",
      name: "Do my weights need to add up to 100 percent?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Ideally yes, but this calculator will still work if they do not. It uses the total weight you entered."
      }
    },
    {
      "@type": "Question",
      name: "Can I add more categories?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Yes. Click the Add category button to include more class categories like projects, labs, or participation."
      }
    }
  ]
};

const webPageSchema = {
  "@context": "https://schema.org",
  "@type": "WebPage",
  name: "Weighted Grade Calculator",
  url: pageUrl,
  description:
    "Calculate your weighted grade using category averages and category weights.",
  isPartOf: {
    "@type": "WebSite",
    name: "Student Performance Toolkit",
    url: siteUrl
  }
};

const breadcrumbSchema = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  itemListElement: [
    {
      "@type": "ListItem",
      position: 1,
      name: "Home",
      item: siteUrl
    },
    {
      "@type": "ListItem",
      position: 2,
      name: "Weighted Grade Calculator",
      item: pageUrl
    }
  ]
};
---

<BaseLayout
  title="Weighted Grade Calculator (Fast & Accurate Category Grade Tool)"
  description="Use this free weighted grade calculator to calculate your class average using category grades and weights."
>
  <Fragment slot="head">
    <script type="application/ld+json" set:html={JSON.stringify(webPageSchema)} />
    <script type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema)} />
    <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
  </Fragment>

  <section class="hero">
    <h1>Weighted Grade Calculator</h1>
    <p>
      Calculate your weighted grade using category averages and category
      weights. Enter homework, quizzes, tests, or any other class category to
      see your overall average instantly.
    </p>
  </section>

  <div class="card">
    <p>
      This weighted grade calculator helps students calculate their class average
      when categories count for different percentages. If tests are worth more
      than homework, this tool gives you a more accurate overall grade than a
      simple average.
    </p>
  </div>

  <div class="grid">
    <section class="card">
      <h2>Enter your categories</h2>

      <div id="category-list">
        <div class="row category-row">
          <div>
            <label for="name-1">Category</label>
            <input id="name-1" class="category-name" type="text" placeholder="Homework" />
          </div>
          <div>
            <label for="grade-1">Category grade (%)</label>
            <input id="grade-1" class="category-grade" type="number" min="0" max="100" step="any" placeholder="92" />
          </div>
          <div>
            <label for="weight-1">Weight (%)</label>
            <input id="weight-1" class="category-weight" type="number" min="0" max="100" step="any" placeholder="20" />
          </div>
        </div>

        <div class="row category-row">
          <div>
            <label for="name-2">Category</label>
            <input id="name-2" class="category-name" type="text" placeholder="Quizzes" />
          </div>
          <div>
            <label for="grade-2">Category grade (%)</label>
            <input id="grade-2" class="category-grade" type="number" min="0" max="100" step="any" placeholder="85" />
          </div>
          <div>
            <label for="weight-2">Weight (%)</label>
            <input id="weight-2" class="category-weight" type="number" min="0" max="100" step="any" placeholder="30" />
          </div>
        </div>

        <div class="row category-row">
          <div>
            <label for="name-3">Category</label>
            <input id="name-3" class="category-name" type="text" placeholder="Tests" />
          </div>
          <div>
            <label for="grade-3">Category grade (%)</label>
            <input id="grade-3" class="category-grade" type="number" min="0" max="100" step="any" placeholder="88" />
          </div>
          <div>
            <label for="weight-3">Weight (%)</label>
            <input id="weight-3" class="category-weight" type="number" min="0" max="100" step="any" placeholder="50" />
          </div>
        </div>
      </div>

      <div class="actions">
        <button id="add-category" type="button" class="btn-secondary">Add category</button>
        <button id="calculate" type="button" class="btn-primary">Calculate weighted grade</button>
      </div>
    </section>

    <aside class="result-box">
      <div class="card">
        <h2>Your result</h2>

        <div class="result-main">
          <p class="muted">Weighted grade</p>
          <div id="weighted-result" class="big-result">--%</div>
          <p id="letter-result"><strong>Letter grade:</strong> --</p>
          <p id="summary-text">Enter your categories and click Calculate weighted grade.</p>
        </div>

        <table class="table">
          <tbody>
            <tr>
              <th>Total weight entered</th>
              <td id="total-weight">--</td>
            </tr>
          </tbody>
        </table>
      </div>
    </aside>
  </div>

  <section class="card" style="margin-top: 1.25rem;">
    <h2>How to Use the Weighted Grade Calculator</h2>
    <ol>
      <li>Enter each category name, such as homework, quizzes, or tests</li>
      <li>Enter your current grade in each category</li>
      <li>Enter the weight of each category</li>
      <li>Click calculate to see your weighted average instantly</li>
    </ol>
  </section>

  <section class="card" style="margin-top: 1.25rem;">
    <h2>How Weighted Grades Work</h2>
    <p>
      A weighted grade gives more importance to some categories than others. For
      example, tests may count much more than homework. That means your overall
      class average should not be found with a simple average.
    </p>
    <p>
      <strong>Example:</strong> If Homework is 92% and worth 20%, Quizzes are
      85% and worth 30%, and Tests are 88% and worth 50%, your weighted grade
      reflects the larger importance of Tests.
    </p>
  </section>

  <section class="card" style="margin-top: 1.25rem;">
    <h2>Frequently Asked Questions</h2>

    <div class="faq-item">
      <h3>How do I calculate a weighted grade?</h3>
      <p>
        Multiply each category grade by its weight, add the results together,
        and divide by the total weight. This calculator does that for you
        automatically.
      </p>
    </div>

    <div class="faq-item">
      <h3>Do my weights need to add up to 100%?</h3>
      <p>
        Ideally yes, but this calculator will still work if they do not. It
        calculates based on the total weight you entered.
      </p>
    </div>

    <div class="faq-item">
      <h3>Can I add more categories?</h3>
      <p>
        Yes. Click the Add category button to include more parts of your class,
        such as labs, projects, or participation.
      </p>
    </div>
  </section>

  <section class="card" style="margin-top: 1.25rem;">
    <h2>Related tools</h2>
    <p>
      Try also:
      <a href="/grade-calculator">Grade Calculator</a> |
      <a href="/final-exam-calculator">Final Exam Calculator</a> |
      <a href="/gpa-calculator">GPA Calculator</a> |
      <a href="/what-grade-do-i-need-calculator">What Grade Do I Need Calculator</a>
    </p>
  </section>

  <script>
    let categoryCount = 3;

    const categoryList = document.getElementById("category-list");
    const addCategoryButton = document.getElementById("add-category");
    const calculateButton = document.getElementById("calculate");

    const weightedResult = document.getElementById("weighted-result");
    const letterResult = document.getElementById("letter-result");
    const summaryText = document.getElementById("summary-text");
    const totalWeightCell = document.getElementById("total-weight");

    function getLetterGrade(percent) {
      if (percent >= 90) return "A";
      if (percent >= 80) return "B";
      if (percent >= 70) return "C";
      if (percent >= 60) return "D";
      return "F";
    }

    function addCategory() {
      categoryCount += 1;

      const row = document.createElement("div");
      row.className = "row category-row";

      row.innerHTML = `
        <div>
          <label for="name-${categoryCount}">Category</label>
          <input id="name-${categoryCount}" class="category-name" type="text" placeholder="Projects" />
        </div>
        <div>
          <label for="grade-${categoryCount}">Category grade (%)</label>
          <input id="grade-${categoryCount}" class="category-grade" type="number" min="0" max="100" step="any" placeholder="90" />
        </div>
        <div>
          <label for="weight-${categoryCount}">Weight (%)</label>
          <input id="weight-${categoryCount}" class="category-weight" type="number" min="0" max="100" step="any" placeholder="10" />
        </div>
      `;

      categoryList.appendChild(row);
    }

    function calculateWeightedGrade() {
      const gradeInputs = document.querySelectorAll(".category-grade");
      const weightInputs = document.querySelectorAll(".category-weight");

      let weightedSum = 0;
      let totalWeight = 0;

      for (let i = 0; i < gradeInputs.length; i++) {
        const grade = parseFloat(gradeInputs[i].value);
        const weight = parseFloat(weightInputs[i].value);

        if (!Number.isNaN(grade) && !Number.isNaN(weight) && weight > 0) {
          weightedSum += grade * weight;
          totalWeight += weight;
        }
      }

      if (totalWeight === 0) {
        weightedResult.textContent = "--%";
        letterResult.innerHTML = "<strong>Letter grade:</strong> --";
        summaryText.textContent = "Please enter at least one valid category and weight.";
        totalWeightCell.textContent = "--";
        return;
      }

      const result = weightedSum / totalWeight;
      const rounded = result.toFixed(1);
      const letter = getLetterGrade(result);

      weightedResult.textContent = `${rounded}%`;
      letterResult.innerHTML = `<strong>Letter grade:</strong> ${letter}`;
      summaryText.textContent = `Your current weighted grade is ${rounded}% (${letter}).`;
      totalWeightCell.textContent = `${totalWeight.toFixed(1).replace(/\.0$/, "")}%`;
    }

    addCategoryButton?.addEventListener("click", addCategory);
    calculateButton?.addEventListener("click", calculateWeightedGrade);
  </script>
</BaseLayout>